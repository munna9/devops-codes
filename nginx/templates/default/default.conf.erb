#<%=node['phenom']['banner']%>
server {
  listen  80;
  server_name localhost;

  location / {
    root  <%=node['nginx']['site']['default_directory']%>;
    index  index.html index.htm;
  }

  error_page  500 502 503 504  /50x.html;
  location = /50x.html {
    root  <%=node['nginx']['site']['default_directory']%>;
  }
}