{
  "name": "mongodb",
  "json_class": "Chef::Role",
  "chef_type": "role",
  "description": "Recipes which configures host to be mongodb server",
  "default_attributes": {

  },
  "override_attributes": {

  },
  "run_list": [
    "role[_base]"
  ],
  "env_run_lists": {
    "dev":[
      "role[_base]",
      "recipe[mongodb::install]",
      "recipe[mongodb::fine_tunning]",
      "recipe[mongodb::configure]",
      "recipe[mongodb::logrotate_install]",
      "recipe[mongodb::service]"
    ],
    "dev-candidates": [
      "role[_base]",
      "recipe[mongodb::install]",
      "recipe[mongodb::fine_tunning]",
      "recipe[mongodb::configure]",
      "recipe[mongodb::logrotate_install]",
      "recipe[mongodb::service]"
    ],
    "qa": [
      "role[_base]",
      "role[_elk-agent]",
      "recipe[mongodb::install]",
      "recipe[mongodb::fine_tunning]",
      "recipe[mongodb::configure]",
      "recipe[mongodb::logrotate_install]",
      "recipe[mongodb::service]"
    ],
    "qa-candidates": [
      "role[_base]",
      "recipe[mongodb::install]",
      "recipe[mongodb::fine_tunning]",
      "recipe[mongodb::configure]",
      "recipe[mongodb::logrotate_install]",
      "recipe[mongodb::service]"
    ],
    "prod-ngccca": [
      "role[_base]",
      "role[_elk-agent]",
      "recipe[mongodb::install]",
      "recipe[mongodb::fine_tunning]",
      "recipe[mongodb::configure]",
      "recipe[mongodb::logrotate_install]",
      "recipe[mongodb::service]"
    ],
    "prod-candidatesca": [
      "role[_base]",
      "role[_elk-agent]",
      "recipe[mongodb::install]",
      "recipe[mongodb::fine_tunning]",
      "recipe[mongodb::configure]",
      "recipe[mongodb::logrotate_install]",
      "recipe[mongodb::service]"
    ]
  }
}
